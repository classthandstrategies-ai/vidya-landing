node_modules/
.next/
out/
.env
.env.local
.env.*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
.vscode/
.idea/

# Stats are generated by `prebuild` from the private repo dataset snapshot.
# We commit the snapshot so the public repo stays self-contained.
# If you regenerate from a newer snapshot, the diff here is expected.
